"use client";
import { ArrowUpRight, Check, LoaderCircle } from "lucide-react";
import { useState } from "react";
export function ContactForm(){const [state,setState]=useState<"idle"|"sending"|"sent">("idle"),[error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState("sending");setError("");const form=new FormData(e.currentTarget);const response=await fetch("/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(form))});const body=await response.json();if(!response.ok){setError(body.error);setState("idle");return}setState("sent");e.currentTarget.reset();}return <form className="contact-form" onSubmit={submit}>{state==="sent"?<div className="contact-sent"><Check/><h3>Message received.</h3><p>Thanks—I'll get back to you soon.</p><button type="button" onClick={()=>setState("idle")}>Send another</button></div>:<><label>Your name<input name="name" required/></label><label>Email address<input name="email" type="email" required/></label><label>What can I help with?<input name="subject"/></label><label>Tell me about it<textarea name="message" rows={4} required/></label>{error&&<p className="contact-error">{error} <a href="mailto:dsandipan3002@gmail.com">Email directly</a></p>}<button type="submit" disabled={state==="sending"}>{state==="sending"?<LoaderCircle className="spin"/>:<ArrowUpRight/>}{state==="sending"?"Sending…":"Send message"}</button></>}</form>}
