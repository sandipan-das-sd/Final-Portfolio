"use client";
import { useEffect } from "react";
export function MotionLayer(){useEffect(()=>{const reduced=matchMedia("(prefers-reduced-motion: reduce)").matches;if(!reduced)document.body.classList.add("motion-ready");const sections=document.querySelectorAll<HTMLElement>(".section");const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("is-visible");observer.unobserve(entry.target)}}),{threshold:.08,rootMargin:"0px 0px -40px"});if(!reduced)sections.forEach(section=>observer.observe(section));const cursor=document.querySelector<HTMLElement>(".custom-cursor");const move=(e:PointerEvent)=>{cursor?.style.setProperty("--cursor-x",`${e.clientX}px`);cursor?.style.setProperty("--cursor-y",`${e.clientY}px`)};const over=(e:PointerEvent)=>cursor?.classList.toggle("is-link",Boolean((e.target as Element).closest("a,button,input,textarea,label")));
const setScrollbarTheme=()=>{const element=document.elementFromPoint(Math.max(1,window.innerWidth-24),window.innerHeight*.5);const section=element?.closest("section");const dark=section?.classList.contains("hero")||section?.classList.contains("sap")||section?.classList.contains("contact");const lime=section?.classList.contains("ai-practice")||section?.classList.contains("resume");document.documentElement.dataset.scrollTheme=dark?"acid":lime?"violet":"glass-green"};let frame=0;const scroll=()=>{cancelAnimationFrame(frame);frame=requestAnimationFrame(setScrollbarTheme)};setScrollbarTheme();window.addEventListener("scroll",scroll,{passive:true});window.addEventListener("resize",scroll,{passive:true});window.addEventListener("pointermove",move);document.addEventListener("pointerover",over);return()=>{observer.disconnect();cancelAnimationFrame(frame);window.removeEventListener("scroll",scroll);window.removeEventListener("resize",scroll);window.removeEventListener("pointermove",move);document.removeEventListener("pointerover",over);document.body.classList.remove("motion-ready")}},[]);return <div className="custom-cursor" aria-hidden="true"><i/></div>}
