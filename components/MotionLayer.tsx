"use client";
import { useEffect } from "react";
export function MotionLayer(){useEffect(()=>{if(matchMedia("(prefers-reduced-motion: reduce)").matches)return;document.body.classList.add("motion-ready");const sections=document.querySelectorAll<HTMLElement>(".section");const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("is-visible");observer.unobserve(entry.target)}}),{threshold:.08,rootMargin:"0px 0px -40px"});sections.forEach(section=>observer.observe(section));const cursor=document.querySelector<HTMLElement>(".custom-cursor");const move=(e:PointerEvent)=>{cursor?.style.setProperty("--cursor-x",`${e.clientX}px`);cursor?.style.setProperty("--cursor-y",`${e.clientY}px`)};const over=(e:PointerEvent)=>cursor?.classList.toggle("is-link",Boolean((e.target as Element).closest("a,button,input,textarea,label")));window.addEventListener("pointermove",move);document.addEventListener("pointerover",over);return()=>{observer.disconnect();window.removeEventListener("pointermove",move);document.removeEventListener("pointerover",over);document.body.classList.remove("motion-ready")}},[]);return <div className="custom-cursor" aria-hidden="true"><i/></div>}
